<Window
	x:Class="LogMonitoringTool.Views.Result.ResultWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="clr-namespace:LogMonitoringTool.ViewModels.Result"
	mc:Ignorable="d"
	Title="{ Binding TitleText }"
	Height="600"
	Width="800"
>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="30" />
		</Grid.RowDefinitions>

		<DataGrid
			Grid.Row="0"
			IsReadOnly="True"
			AutoGenerateColumns="False"
			ItemsSource="{ Binding AnalysisResultItems }" >
			<DataGrid.Columns>
				<DataGridTextColumn
					Header="解析前ログ" 
					Binding="{ Binding BeforeTextLine }" />
				<DataGridTextColumn
					Header="解析結果ログ" 
					Binding="{ Binding AfterTextLine }" />
			</DataGrid.Columns>
		</DataGrid>

		<Button
			Grid.Row="1" 
			Content="{ Binding CloseButtonContent }" />

	</Grid>

</Window>
