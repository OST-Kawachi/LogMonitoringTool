<Window
	x:Class="LogMonitoringTool.Views.Result.ResultWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="clr-namespace:LogMonitoringTool.ViewModels.Result"
	mc:Ignorable="d"
	Title="{ Binding TitleText }"
	Height="600"
	Width="800"
>

	<Window.DataContext>
		<vm:ResultViewModel />
	</Window.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition />
			<RowDefinition Height="30" />
		</Grid.RowDefinitions>

		<Grid
			Grid.Row="0" >
			<Grid.RowDefinitions>
				<RowDefinition
					Height="30" />
				<RowDefinition />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<Label
				Grid.Row="0"
				Grid.Column="0"
				Content="{ Binding BeforeLogLabelContent }" />

			<ScrollViewer
				Grid.Row="1"
				Grid.Column="0">
				<TextBox />
			</ScrollViewer>

			<Label
				Grid.Row="0"
				Grid.Column="1"
				Content="{ Binding AfterLogLabelContent }" />

			<ScrollViewer
				Grid.Row="1"
				Grid.Column="1">
				<TextBox />
			</ScrollViewer>

		</Grid>

		<Button
			Grid.Row="1" 
			Content="{ Binding CloseButtonContent }" />

	</Grid>

</Window>
